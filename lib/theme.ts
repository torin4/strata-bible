// The browser-chrome / splash color, mirrored to the page <meta name="theme-color"> and
// the PWA manifest. It must be a literal hex (a viewport config and a JSON manifest cannot
// read a CSS variable), so it lives here, in sync with the --shell token in tokens.css
// (5 6 10) and the manifest's theme_color/background_color. Change all three together.
export const THEME_COLOR = "#05060a";
